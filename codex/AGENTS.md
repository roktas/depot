# Codex Instructions

Use the shared user instructions from `~/.agents/AGENTS.md`.

Codex-specific assets live under `~/.codex`. Prefer `~/.codex/skills` for Codex-only skills and `~/.codex/hooks` for
Codex hooks. Do not mirror shared `~/.agents/skills` entries here.
