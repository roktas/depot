FROM ubuntu:24.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update \
  && apt-get install -y --no-install-recommends ca-certificates git ruby \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /repo

CMD ["/repo/.agents/tests/provision/smoke.sh"]
