interface:
  display_name: "Depot"
  short_description: "Plan and apply this repo's provisioning"
  default_prompt: "Use $depot to choose the right provisioning action, show the plan, and ask before applying it."
policy:
  allow_implicit_invocation: true
